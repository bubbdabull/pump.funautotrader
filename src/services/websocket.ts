/**
 * @deprecated Import `realtimeGateway` from `@/services/realtime-gateway` instead.
 * This module re-exports the single app-wide Socket.IO connection.
 */
export { realtimeGateway, wsService } from '@/services/realtime-gateway'
