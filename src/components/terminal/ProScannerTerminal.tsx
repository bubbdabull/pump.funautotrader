import { PhotonTerminal } from '@/components/terminal/photon/PhotonTerminal'

/** Pro Scanner — Photon-style dense trading terminal (frontend only). */
export function ProScannerTerminal() {
  return <PhotonTerminal />
}
